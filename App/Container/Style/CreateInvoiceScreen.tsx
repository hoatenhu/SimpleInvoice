import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    marginTop: 10,
  },
  datePickerView: {
    width: '100%',
    alignItems: 'center',
    marginBottom: 10,
  },
});
